# Types

This directory is now deprecated. TypeScript types have been reorganized into the models directory:
- API types are in src/models/api-types.ts
- Domain models are in src/models/data-objects.ts