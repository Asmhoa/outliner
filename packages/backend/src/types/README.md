# Types

TypeScript type definitions for the backend application.

This contains custom TypeScript types, interfaces, and type declarations that are used throughout the application to ensure type safety.