# Database

Database operations and management for the backend application.

This contains classes and functions for interacting with the SQLite databases, including the system database (which tracks user databases) and user databases (which store pages, blocks, and workspaces). It also includes database-specific error handling.