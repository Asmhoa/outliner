# Routes

API route definitions for the backend application.

This contains Express route handlers that correspond to the REST API endpoints, organized by functionality (pages, blocks, databases, workspaces, etc.). These routes handle incoming HTTP requests and return appropriate responses.