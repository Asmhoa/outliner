# @outliner/backend

Backend API for Outliner application

## Overview

This package contains the backend API server for the Outliner application. It provides REST APIs for managing pages, blocks, and workspaces using a SQLite database for data persistence.

## Features

- REST API endpoints for managing pages, blocks, and workspaces
- SQLite database for data persistence
- CORS support for web frontend integration